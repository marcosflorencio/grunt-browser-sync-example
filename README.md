# grunt-browser-sync-example
Exemplo de browser-sync utlizando uma task no grunt

## Install

```
$ npm install
$ npm install -g grunt-cli (caso nunca tenha usado o grunt)
```

## Usage
```
$ grunt browser-sync
Abra http://localhost:3000 :)
```
